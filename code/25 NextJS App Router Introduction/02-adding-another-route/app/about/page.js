// New routes can be added by adding a folder with the route name and defining a 'page.js' file
// in it which will be executed on the server to render the page.
export default function AboutPage() {
  return (
    <main>
      <h1>About Us</h1>
    </main>
  );
}
